# --- Upstream Hermes (cloned by scripts/install.ps1, pinned in HERMES_PIN.txt) ---
/hermes-agent/

# --- Python / uv venv ---
.venv/
__pycache__/
*.pyc
*.egg-info/

# --- Node ---
node_modules/

# --- HERMES_HOME — entirely runtime/generated (config + persona are copies of
# vr-overlay; skills/sessions/memory/secrets all live here). Regenerated by
# scripts/install.ps1 + sync-overlay.ps1. Never committed.
/home/

# --- misc ---
.clone.log
*.log
.readme-drafts/
